# Grok adapters

Thin fail-closed adapters live here (or as Grok Bot skills pointing here).

Rules:
1. Git repo (this tree) is the source of truth.
2. Grok must `Read` `../ars-codex/SKILL.md` / workflows and `../paper-spine/SKILL.md` + `references/` before acting.
3. Run gate scripts from `../paper-spine/scripts/` — do not skip gates or invent artifacts.
4. Do not treat chat memory as a substitute for these files.
